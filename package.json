{
  "name": "quarto",
  "version": "1.0.0",
  "dependencies": {
    "concurrently": "^4.0.1",
    "express": "^4.16.2",
    "global": "^4.3.2",
    "nodemon": "^1.17.3",
    "socket.io": "^2.1.0"
  },
  "scripts": {
    "server": "node server.js",
    "client": "npm run start --prefix quarto",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "heroku-postbuild": "NPM_PRODUCTION=false npm install --prefix quarto && npm run build --prefix quarto"
  }
}
